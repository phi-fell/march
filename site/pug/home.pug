doctype html
html
    head
        title GotG
        script(src=socket_io_path)
        script(src=jquery_path)
        script(src=vue_path)
        script(type="module" src="./js/home.js")
        link(rel="stylesheet" type="text/css" href="./css/global.css")
        link(rel="stylesheet" type="text/css" href="./css/home.css")

body
    #home.all
        .row
            .section.flex_none
                .col
                    p Players ({{players.length}}/{{MAX_PLAYERS}})
                    player.row(v-for="player in players" v-bind:key="player.id" v-bind:self="player")
                    .row
                        .flex
                        button#new_player_button New Player
            .flex
